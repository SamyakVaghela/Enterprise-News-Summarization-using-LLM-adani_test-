# AI News Summarization using BART-Large

This project uses the **BART-Large-XSum** model to perform abstractive summarization on news articles.

## How to Run Locally
1. Install dependencies: `pip install -r requirements.txt`
2. Run the app: `streamlit run app.py`
